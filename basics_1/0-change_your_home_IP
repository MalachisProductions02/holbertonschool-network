#!/usr/bin/env bash

# Backup the original hosts file (for safety)
cp /etc/hosts /etc/hosts.bak

# Replace localhost with 127.0.0.2 and add facebook.com with 8.8.8.8
# Overwrite the hosts file
echo "127.0.0.2 localhost" > /etc/hosts
echo "8.8.8.8 facebook.com" >> /etc/hosts
